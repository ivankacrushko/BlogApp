@model BlogApp.Models.ApplicationUser
<h1>@Model.FullName's Profile</h1>
<h2>Posts:</h2>
@foreach (var post in Model.Posts)
{
    <div>
        <h3>@post.Title</h3>
        <p>@post.Content</p>
    </div>
}
